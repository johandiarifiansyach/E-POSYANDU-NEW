# Only the embedded persistence bridge needs a database driver.  Transport is
# served by Rust/tonic, so grpcio is intentionally not installed here.
psycopg[binary]==3.2.9
# Used for vectorized WHO/LMS batch calculations and chart curve generation.
# Set ANALYSIS_NUMPY_ENABLED=false for a controlled scalar fallback.
numpy>=1.26,<3
